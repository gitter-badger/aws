general:
  branches:
    only:
      - master
      - dev
      - /feature-.*/
      - /test-.*/

machine:
  services:
    - docker

dependencies:
  override:
    - sudo scripts/run.sh:
        timeout: 300

test:
  override:
    - sudo scripts/validate.sh:
        timeout: 300
